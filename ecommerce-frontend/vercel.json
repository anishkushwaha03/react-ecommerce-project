{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "${BACKEND_URL}/api/:path*"
    },
    {
      "source": "/images/:path*",
      "destination": "${BACKEND_URL}/images/:path*"
    }
  ]
}